# Brand Asset Placeholders

Replace these files before production launch:

- `logo-placeholder.svg` -> official Radar Puls logo
- `og-placeholder.svg` -> 1200x630 social sharing card

Checklist:
1. Keep file names or update metadata references in `app/[locale]/layout.tsx`.
2. Ensure OG image is exactly 1200x630.
3. Verify contrast and readability on mobile previews.
