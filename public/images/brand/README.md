# Brand Assets

Canonical files:

- `logo.svg` -> official Radar Puls logo
- `../banner1.png` -> landing and marketing banner
- `og-default.png` -> 1200x630 social sharing card

Supporting placeholders:

- `logo-placeholder.svg` -> legacy placeholder
- `og-placeholder.svg` -> legacy placeholder

Checklist:
1. Keep file names or update metadata references in `app/[locale]/layout.tsx`.
2. Ensure OG image is exactly `1200x630`.
3. Verify contrast and readability on mobile and social previews.
